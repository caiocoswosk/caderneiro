Leia a primeira linha de `instrucoes/atualizar-caderno.md` para extrair o nível do modelo (`<!-- modelo: NIVEL -->`). Consulte `instrucoes/modelos.md` para verificar se o modelo ativo corresponde ao nível recomendado. Se o modelo ativo for **diferente** do recomendado, sugira a troca via `/model` e **pare — aguarde a decisão do usuário** antes de prosseguir. Se compatível, prossiga sem comentários.

Em seguida, leia `instrucoes/atualizar-caderno.md` por completo e execute a operação.
